spring:
  profiles:
      include: oauth
  session:
    store-type: jdbc

  jpa:
    show-sql: true
    hibernate:
      dialect: org.hibernate.dialect.MySQL5InnoDBDialect

  h2:
    console:
      enabled: true