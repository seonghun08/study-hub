spring:
  jpa:
    hibernate:
      ddl-auto: create
    properties:
      hibernate:
        # show_sql: true
        format_sql: true
        default_batch_fetch_size: 100

    open-in-view: true

logging.level:
  org.hibernate.SQL: debug
#  org.hibernate.type: trace